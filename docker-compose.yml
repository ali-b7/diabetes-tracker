version: "3.9"

services:
  diabetes-app:
    build: .
    container_name: diabetes-app
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=change-me-in-prod
    volumes:
      - diabetes_data:/app/instance
    restart: unless-stopped

volumes:
  diabetes_data:
